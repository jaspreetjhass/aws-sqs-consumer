spring:  application:    name: aws-sqs-consumerserver:  port: 8090
aws:  credentials:    access-key: ${ACCESS_KEY}    secret-key: ${SECRET_KEY}    region: ${REGION}  services:    auto-configuration-enabled: true    s3:      enabled: false    rds:      enabled: false    sns:      enabled: false      sqs:      enabled: false      async-enabled: true      queue-url: ${QUEUE_URL}